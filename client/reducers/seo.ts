let initSEO = {
    // title: "",
    // description: "",
    // keywords: ""
}

export const seo = (state = {}, action: any): typeof initSEO => {
    switch (action.type) {
        default:
            return state;
    }
}